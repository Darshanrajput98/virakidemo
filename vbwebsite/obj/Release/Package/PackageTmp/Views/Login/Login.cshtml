@{
    Layout = null;
}

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Viraki Brothers | Log in</title>
    <!-- Tell the browser to be responsive to screen width -->
    <meta content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no" name="viewport">
    <!-- Bootstrap 3.3.6 -->
    <link rel="stylesheet" href="../../bootstrap/css/bootstrap.min.css">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.5.0/css/font-awesome.min.css">
    <!-- Ionicons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/ionicons/2.0.1/css/ionicons.min.css">
    <!-- Theme style -->
    <link rel="stylesheet" href="../../dist/css/AdminLTE.min.css">
    <!-- iCheck -->
    <link rel="stylesheet" href="../../plugins/iCheck/square/blue.css">
    <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
    <script src="https://oss.maxcdn.com/html5shiv/3.7.3/html5shiv.min.js"></script>
    <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
</head>
<body class="hold-transition login-page">
    @if (Session["Logincheck"] != "false")
    {
        <div class="alert alert-danger" id="sucessalert" role="alert" style="display:block;">Your username or password is incorrect, please verify it!!!!</div>
    }


    <div class="login-box">
        <div class="login-logo">
            <a href="#"><b>Viraki</b>Brothers</a>
        </div>
        <!-- /.login-logo -->
        @using (Html.BeginForm("Login", "Login", FormMethod.Post, FormMethod.Post, new { enctype = "multipart/form-data" }))
        {
            <div class="login-box-body">
                <p class="login-box-msg">Sign in to start your session</p>
                <div>
                    <div class="form-group has-feedback">
                        <input type="text" class="form-control" name="UserName" placeholder="Username" required>
                        <span class="glyphicon glyphicon-user form-control-feedback"></span>
                    </div>
                    <div class="form-group has-feedback">
                        <input type="password" class="form-control" name="Password" placeholder="Password" required>
                        <span class="glyphicon glyphicon-lock form-control-feedback"></span>
                    </div>
                    <div class="row">
                        <div class="col-xs-8">
                            <div class="checkbox icheck">
                                <label>
                                    <input type="checkbox"> Remember Me
                                </label>
                            </div>
                        </div>
                        <!-- /.col -->
                        <div class="col-xs-4">
                            <button type="submit" class="btn btn-primary btn-block btn-flat" value="Login">Sign In</button>
                            @*<input type="button" id="btnsavecase" class="btn btn-primary btn-block btn-flat" value="Create User">*@
                            @*<input type="button" id="UpdateInvoiceTotal" class="btn btn-primary btn-block btn-flat" value="Invoice Total">*@
                            @*<input type="button" id="UpdateRetInvoiceTotal" class="btn btn-primary btn-block btn-flat" value="Invoice Total">*@
                            @*<input type="button" id="UpdateRetVehicleAssignedDate" class="btn btn-primary btn-block btn-flat" value="DATE">*@
                            @*<input type="button" id="UpdateRetCreditmemo" class="btn btn-primary btn-block btn-flat" value="DATE">*@


                            @*<input type="button" id="UpdateCellNo" class="btn btn-primary btn-block btn-flat" value="UpdateCellNo">*@

                        </div>
                        <!-- /.col -->
                    </div>
                </div>

            </div>

        }

    </div>

    <script src="~/Scripts/jquery-1.10.2.js"></script>


    <script>

        $(document).ready(function () {
            loadhtml();
        });


        @*$("#btnsavecase").click(function () {
            $.ajax({
                url: '@Url.Action("CreatePasswrd", "Admin", new { area = "wholesale" })',
                type: 'POST',
                cache: false,
            }).done(function (result) {
                if (result == true) {
                    if ($("#AreaID").val() == "") {
                        $('div#sucessalert').html("Area Add successfully.");
                    }
                    else {
                        $('div#sucessalert').html("Area updated successfully.");
                    }
                    showhidealert();
                    loadhtml();
                    clearform();
                }
            });
        });

        $("#UpdateInvoiceTotal").click(function () {
            debugger;
            $.ajax({
                url: '@Url.Action("UpdateInvoiceTotal", "Admin", new { area = "wholesale" })',
                type: 'POST',
                cache: false,
            }).done(function (result) {
                if (result == true) {
                    if ($("#AreaID").val() == "") {
                        $('div#sucessalert').html("Area Add successfully.");
                    }
                    else {
                        $('div#sucessalert').html("Area updated successfully.");
                    }
                    showhidealert();
                    loadhtml();
                    clearform();
                }
            });
        });

        $("#UpdateRetInvoiceTotal").click(function () {
            debugger;
            $.ajax({
                url: '@Url.Action("UpdateRetInvoiceTotal", "Admin", new { area = "wholesale" })',
                type: 'POST',
                cache: false,
            }).done(function (result) {
                if (result == true) {
                    if ($("#AreaID").val() == "") {
                        $('div#sucessalert').html("Area Add successfully.");
                    }
                    else {
                        $('div#sucessalert').html("Area updated successfully.");
                    }
                    showhidealert();
                    loadhtml();
                    clearform();
                }
            });
        });*@


        @*$("#UpdateRetVehicleAssignedDate").click(function () {
            debugger;
            $.ajax({
                url: '@Url.Action("UpdateRetVehicleAssignedDate", "Admin", new { area = "wholesale" })',
                type: 'POST',
                cache: false,
            }).done(function (result) {
                if (result == true) {
                    if ($("#AreaID").val() == "") {
                        $('div#sucessalert').html("Area Add successfully.");
                    }
                    else {
                        $('div#sucessalert').html("Area updated successfully.");
                    }
                    showhidealert();
                    loadhtml();
                    clearform();
                }
            });
        });*@

        @*$("#UpdateRetCreditmemo").click(function () {
            debugger;
            $.ajax({
                url: '@Url.Action("UpdateRetCreditmemo", "Admin", new { area = "wholesale" })',
                type: 'POST',
                cache: false,
            }).done(function (result) {
                if (result == true) {
                    if ($("#AreaID").val() == "") {
                        $('div#sucessalert').html("Area Add successfully.");
                    }
                    else {
                        $('div#sucessalert').html("Area updated successfully.");
                    }
                    showhidealert();
                    loadhtml();
                    clearform();
                }
            });
        });

        // For Wholesale
        $("#UpdateCellNo").click(function () {
            debugger;
            $.ajax({
                url: '@Url.Action("UpdateCellNo", "Admin", new { area = "wholesale" })',
                type: 'POST',
                cache: false,
            }).done(function (result) {
                if (result == true) {
                        $('div#sucessalert').html("Area Add successfully.");
                    showhidealert();
                    loadhtml();
                    clearform();
                }
            });
        });*@

        // For Retail
        @*$("#UpdateCellNo").click(function () {
            debugger;
            $.ajax({
                url: '@Url.Action("UpdateCellNoRetail", "Admin", new { area = "wholesale" })',
                type: 'POST',
                cache: false,
            }).done(function (result) {
                if (result == true) {
                    $('div#sucessalert').html("Area Add successfully.");
                    showhidealert();
                    loadhtml();
                    clearform();
                }
            });
        });*@



        $("#CancelArea").click(function () {
            clearform();
        });

        function clearform() {
            $("#frmArea").find("input").val("");
            $("#AreaID").val("").trigger("change");
            $("#DaysofWeek").val("").trigger("change");
        }
    </script>










    <!-- /.login-box -->
    <!-- jQuery 2.2.0 -->
    <script src="../../plugins/jQuery/jQuery-2.2.0.min.js"></script>
    <!-- Bootstrap 3.3.6 -->
    <script src="../../bootstrap/js/bootstrap.min.js"></script>
    <!-- iCheck -->
    <script src="../../plugins/iCheck/icheck.min.js"></script>
    <script>
        $(function () {
            $('input').iCheck({
                checkboxClass: 'icheckbox_square-blue',
                radioClass: 'iradio_square-blue',
                increaseArea: '20%' // optional
            });
        });
    </script>
</body>
</html>



